{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Desktop window capabilities",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "notification:default",
    {
      "identifier": "shell:allow-spawn",
      "allow": [{ "name": "binaries/alphonse-daemon", "sidecar": true }]
    }
  ]
}
