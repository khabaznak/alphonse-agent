# Available Tools

Select tools by capability group first, then choose the narrowest tool that solves the request.
{% for g in groups %}

---

## {{ g.title }}

**Choose this group when:** {{ g.when_to_choose }}

**Avoid this group when:** {{ g.avoid_when }}

{% for t in g.tools %}
### `{{ t.key }}`

**What it does:** {{ t.description }}

**Use when:** {{ t.when_to_use if t.when_to_use else "Use when this is the most direct deterministic tool for the task." }}

**Avoid when:** {% if t.safety_level in ["high", "critical"] %}A lower-risk tool can satisfy the same request.{% else %}Another tool provides a more specific or safer fit.{% endif %}

**Returns:** {{ t.returns if t.returns else "tool-specific payload" }}

**Domains:** {{ t.domain_tags | join(", ") if t.domain_tags else "—" }}

**Safety Level:** {{ t.safety_level }}{% if t.requires_confirmation %} (Requires confirmation before execution){% endif %}

**Input Schema (JSON):**
```json
{{ t.input_schema_pretty }}
```
{% if t.examples %}
**Example Call:**
```json
{{ t.examples[0] | tojson(indent=2) }}
```
{% endif %}
{% endfor %}
{% endfor %}
